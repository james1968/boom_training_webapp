<%= form_for(@friendship, as: :friendship, url: user_friendships_path(current_user), method: :post) do |f| %>
	<%= f.label :second_user_email %>
	<%= f.email_field :second_user_email %>
	<%= f.submit "Add Friend" %>
<% end %>